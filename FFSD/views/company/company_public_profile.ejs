<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= company.companyName %> - Company Profile</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #3498db;
            --accent-color: #1abc9c;
            --background-color: #f5f7fa;
            --card-bg: #ffffff;
            --text-color: #333333;
            --text-light: #7f8c8d;
            --border-color: #e0e0e0;
            --shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
            --transition: all 0.3s ease;
        }
        body {
            font-family: "Poppins", sans-serif;
            background-color: var(--background-color);
            color: var(--text-color);
            line-height: 1.6;
        }
        .profile-container {
            max-width: 1200px;
            margin: 2rem auto;
            background-color: var(--card-bg);
            border-radius: 16px;
            box-shadow: var(--shadow);
            overflow: hidden;
        }
        .profile-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 3rem 2rem;
            text-align: center;
            position: relative;
        }
        .profile-header h1 {
            font-size: 2.5rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        .profile-header p {
            font-size: 1.1rem;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 1.5rem;
        }
        .profile-body {
            padding: 0 2rem 2rem 2rem; /* Adjusted padding */
        }
        .profile-section {
            margin-bottom: 2.5rem;
        }
        .section-title {
            font-size: 1.75rem;
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 1.5rem;
            padding-bottom: 0.75rem;
            border-bottom: 2px solid var(--accent-color);
            display: inline-block;
        }
        .company-details-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
        }
        .detail-item {
            background-color: #f9fafb;
            padding: 1rem;
            border-radius: 8px;
            border-left: 4px solid var(--secondary-color);
        }
        .detail-label {
            font-size: 0.9rem;
            color: var(--text-light);
            margin-bottom: 0.25rem;
        }
        .detail-value {
            font-size: 1rem;
            font-weight: 500;
        }
        .team-grid, .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }
        .team-card, .project-card {
            background-color: var(--card-bg);
            border-radius: 12px;
            box-shadow: var(--shadow);
            overflow: hidden;
            transition: var(--transition);
        }
        .team-card:hover, .project-card:hover {
            transform: translateY(-5px);
        }
        .team-card img, .project-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        .card-content {
            padding: 1.5rem;
        }
        .card-content h3 {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }
        .card-content p {
            font-size: 0.95rem;
            color: var(--text-secondary);
        }
        .openings-list li {
            background-color: #f9fafb;
            padding: 0.75rem 1.25rem;
            border-radius: 8px;
            margin-bottom: 0.75rem;
            font-weight: 500;
        }
        .apply-section {
            text-align: center;
            padding: 2rem;
            background-color: #f9fafb;
            margin: 2rem -2rem -2rem -2rem;
            border-radius: 0 0 16px 16px;
        }
        .btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 500;
            font-size: 0.95rem;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        .btn-primary {
            background-color: var(--accent-color);
            color: white;
            font-size: 1.1rem;
            padding: 0.8rem 2rem;
        }
        .btn-primary:hover {
            background-color: #16a085;
            transform: translateY(-2px);
        }
        .back-button-container {
            padding: 1.5rem 2rem 0 0; /* Adjusted padding */
        }
        .go-back-btn {
            background-color: #f1f5f9;
            color: var(--text-color);
            border: 1px solid var(--border-color);
            font-weight: 500;
            float: right; /* Position button to the right */
        }
        .go-back-btn:hover {
            background-color: #e2e8f0;
        }
    </style>
</head>
<body>
    <%- include('../worker/worker_navbar') %>
    <div class="profile-container">
        <header class="profile-header">
            <h1><%= company.companyName %></h1>
            <% if (company.description) { %>
                <p><%= company.description %></p>
            <% } %>
        </header>

        <main class="profile-body">
            <div class="back-button-container">
                 <button class="btn go-back-btn" onclick="window.history.back()">
                    <i class="fas fa-arrow-left"></i> Go Back
                </button>
            </div>
           
            <!-- About Section: Only shows if 'aboutCompany' data exists -->
            <% if (company.aboutCompany) { %>
                <section class="profile-section">
                    <h2 class="section-title">About Us</h2>
                    <p><%= company.aboutCompany %></p>
                </section>
            <% } %>

            <!-- Company Details Section -->
            <section class="profile-section">
                <h2 class="section-title">Company Details</h2>
                <div class="company-details-grid">
                    <% if (company.location && company.location.city) { %>
                        <div class="detail-item">
                            <div class="detail-label">Location</div>
                            <div class="detail-value"><%= company.location.city %>, <%= company.location.state || '' %></div>
                        </div>
                    <% } %>
                    <% if (company.size) { %>
                        <div class="detail-item">
                            <div class="detail-label">Company Size</div>
                            <div class="detail-value"><%= company.size %> employees</div>
                        </div>
                    <% } %>
                    <% if (company.yearsInBusiness) { %>
                        <div class="detail-item">
                            <div class="detail-label">Years in Business</div>
                            <div class="detail-value"><%= company.yearsInBusiness %> years</div>
                        </div>
                    <% } %>
                    <% if (company.specialization && company.specialization.length > 0) { %>
                        <div class="detail-item">
                            <div class="detail-label">Specializations</div>
                            <div class="detail-value"><%= company.specialization.join(', ') %></div>
                        </div>
                    <% } %>
                </div>
            </section>

            <!-- Completed Projects Section: Only shows if there are projects -->
            <% if (company.completedProjects && company.completedProjects.length > 0) { %>
                <section class="profile-section">
                    <h2 class="section-title">Our Projects</h2>
                    <div class="projects-grid">
                        <% company.completedProjects.forEach(project => { %>
                            <div class="project-card">
                                <img src="<%= project.image || 'https://via.placeholder.com/400x200?text=Project+Image' %>" alt="<%= project.title %>">
                                <div class="card-content">
                                    <h3><%= project.title %></h3>
                                    <p><%= project.description %></p>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </section>
            <% } %>

            <!-- Team Section: Only shows if there are team members -->
            <% if (company.teamMembers && company.teamMembers.length > 0) { %>
                <section class="profile-section">
                    <h2 class="section-title">Meet Our Team</h2>
                     <div class="team-grid">
                        <% company.teamMembers.forEach(member => { %>
                            <div class="team-card">
                                <img src="<%= member.image || 'https://via.placeholder.com/400x200?text=Team+Member' %>" alt="<%= member.name %>">
                                <div class="card-content">
                                    <h3><%= member.name %></h3>
                                    <p><%= member.position %></p>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </section>
            <% } %>

            <!-- Openings Section: Only shows if there are openings -->
            <% if (company.currentOpenings && company.currentOpenings.length > 0) { %>
                <section class="profile-section">
                    <h2 class="section-title">Current Openings</h2>
                     <ul class="openings-list">
                        <% company.currentOpenings.forEach(opening => { %>
                            <% if (opening) { %> <li><%= opening %></li> <% } %>
                        <% }) %>
                    </ul>
                </section>
            <% } %>

             <!-- Apply Now Section -->
            <section class="apply-section">
                 <h2>Interested in Joining Us?</h2>
                 <p style="margin: 1rem 0;">Click the button below to go to the application page.</p>
                 <a href="/workerjoin_company" class="btn btn-primary">Apply Now</a>
            </section>
        </main>
    </div>
</body>
</html>

