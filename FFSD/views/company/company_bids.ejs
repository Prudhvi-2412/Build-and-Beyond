<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bidding System</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap"
      rel="stylesheet"
    />
    <style>
    :root {
        /* --- "Deep Dive" Dark Theme Palette --- */
        --primary-accent: #2DD4BF; /* Vibrant Teal */
        --primary-accent-darker: #14B8A6;
        --background-dark: #111827; /* Deep Charcoal Blue */
        --content-dark: #1F2937;   /* Lighter shade for cards */
        --border-dark: #374151;
        --text-primary: #F9FAFB;   /* Off-white for readability */
        --text-secondary: #9CA3AF; /* Light grey for secondary info */
        --text-dark: #111827;

        /* Status Colors for Dark Mode */
        --status-warning-bg: rgba(251, 191, 36, 0.1);
        --status-warning-text: #FBBF24; /* Amber */
        --status-success-bg: rgba(16, 185, 129, 0.1);
        --status-success-text: #10B981; /* Green */
        --status-danger-bg: rgba(239, 68, 68, 0.1);
        --status-danger-text: #EF4444; /* Red */
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }

    body, html {
    height: 100%;
    width: 100%;
    margin: 0;
    padding: 0;
   }

    body {
        background-color: var(--background-dark); /* THEME CHANGE */
        color: var(--text-primary);
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
    }

    .container {
    width: 100%;            /* take full width */
    height: 100%;           /* take full height */
    margin: 0;              /* remove centered margin */
    padding: 2rem;          /* keep some inner spacing */
    border-radius: 0;       /* remove rounded corners if you want flush edges */
    box-sizing: border-box; /* make padding not add extra width */
}

    .nav-menu {
        display: flex;
        margin-bottom: 2rem;
        background-color: var(--background-dark); /* Darker inset look */
        border-radius: 8px;
        overflow: hidden;
        box-shadow: none;
        border: 1px solid var(--border-dark);
    }

    .nav-item {
        padding: 15px 25px;
        cursor: pointer;
        transition: background-color 0.2s, color 0.2s;
        font-weight: 500;
        color: var(--text-secondary); /* THEME CHANGE */
        border-bottom: 3px solid transparent; /* Space for active indicator */
    }

    .nav-item:hover {
        background-color: var(--border-dark); /* THEME CHANGE */
        color: var(--text-primary);
    }

    /* ATTRACTIVE CHANGE: Modern active state for tabs */
    .nav-item.active {
        background-color: transparent;
        color: var(--primary-accent);
        border-bottom: 3px solid var(--primary-accent);
    }

    .main-content {
        position: relative;
    }

    .content-section {
        display: none;
    }
    .content-section.active {
        display: block;
    }

    .section-container {
        display: flex;
        gap: 2rem;
    }

    .left-section, .right-section {
        flex: 1;
        /* Removed individual backgrounds and shadows to unify the look */
        background-color: transparent;
        border-radius: 0;
        padding: 0;
        box-shadow: none;
    }

    .left-section h2, .right-section h2 {
        margin-bottom: 1.5rem;
        color: var(--text-primary);
        font-size: 1.4rem;
    }

    .bid-item {
        background-color: transparent;
        border-radius: 8px;
        padding: 1.25rem;
        margin-bottom: 1rem;
        cursor: pointer;
        transition: transform 0.2s, box-shadow 0.2s, background-color 0.2s;
        border: 1px solid var(--border-dark);
    }

    .bid-item:hover {
        transform: translateY(-3px);
        background-color: var(--border-dark);
        box-shadow: 0 0 15px rgba(45, 212, 191, 0.1);
    }

    .bid-item.active {
        border-left: 4px solid var(--primary-accent);
        background-color: var(--border-dark);
    }

    .bid-title {
        font-weight: 600;
        color: var(--text-primary);
        font-size: 1.1rem;
        margin-bottom: 0.5rem;
    }

    .bid-price {
        color: var(--primary-accent);
        font-weight: 600;
        margin-bottom: 0.75rem;
    }

    .bid-description {
        color: var(--text-secondary);
        margin-bottom: 0.75rem;
        line-height: 1.6;
    }

    .bid-date {
        color: var(--text-secondary);
        font-size: 0.8rem;
    }

    .bid-details-placeholder {
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--text-secondary);
        font-style: italic;
        border: 2px dashed var(--border-dark);
        border-radius: 8px;
    }

    .bid-details-content h2 {
        color: var(--text-primary);
        margin-bottom: 0.75rem;
    }

    .detail-price {
        color: var(--primary-accent);
        font-weight: 600;
        font-size: 1.25rem;
        margin-bottom: 1rem;
    }

    .detail-description, .detail-date {
        color: var(--text-secondary);
        line-height: 1.6;
        margin-bottom: 1rem;
    }

    .additional-info, .bid-form {
        background-color: var(--background-dark);
        border-radius: 8px;
        padding: 1.5rem;
        margin-bottom: 1.5rem;
        border: 1px solid var(--border-dark);
        box-shadow: none;
    }

    .additional-info h3, .bid-form h3 {
        color: var(--text-primary);
        margin-bottom: 1rem;
        font-size: 1.1rem;
    }

    .form-group {
        margin-bottom: 1rem;
    }

    .form-group label {
        display: block;
        margin-bottom: 0.5rem;
        color: var(--text-secondary);
        font-size: 0.9rem;
    }

    .form-group input, .form-group textarea {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid var(--border-dark);
        border-radius: 6px;
        font-size: 1rem;
        background-color: var(--background-dark);
        color: var(--text-primary);
        transition: border-color 0.2s;
    }

    .form-group input:focus, .form-group textarea:focus {
        outline: none;
        border-color: var(--primary-accent);
    }

    .btn-primary {
        background-color: var(--primary-accent);
        color: var(--text-dark);
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 6px;
        cursor: pointer;
        font-size: 1rem;
        font-weight: 600;
        transition: all 0.2s;
    }

    .btn-primary:hover {
        background-color: var(--primary-accent-darker);
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(45, 212, 191, 0.2);
    }

    .bid-status-container {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .status-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: var(--content-dark);
        border: 1px solid var(--border-dark);
        border-radius: 8px;
        padding: 1.5rem;
        box-shadow: none;
    }

    .status-title, .status-price, .status-description, .status-date, .your-bid {
        color: var(--text-primary);
    }
    .status-price { color: var(--primary-accent); }
    .status-description, .status-date { color: var(--text-secondary); }

    /* THEME CHANGE: Dark mode status badges */
    .status-badge {
        padding: 0.5rem 1rem;
        border-radius: 50px;
        font-weight: 600;
        min-width: 100px;
        text-align: center;
        font-size: 0.8rem;
    }

    .status-badge.pending {
        background-color: var(--status-warning-bg);
        color: var(--status-warning-text);
    }

    .status-badge.accepted {
        background-color: var(--status-success-bg);
        color: var(--status-success-text);
    }

    .status-badge.rejected {
        background-color: var(--status-danger-bg);
        color: var(--status-danger-text);
    }
</style>
</head>
<body>
    <%- include('navbar_company') %>
    <div class="container">

        <div class="nav-menu">
            <div class="nav-item active" data-section="place-bid">Place a Bid</div>
            <div class="nav-item" data-section="bid-status">Bid Status</div>
        </div>


        <div class="main-content">

          <!-- PLACE BID SECTION -->
          <div id="place-bid" class="content-section active">
              <% if (typeof req !== 'undefined') { %>
                  <% if (req.query.success === 'bid_submitted') { %>
                      <div class="alert alert-success">Your bid has been submitted successfully!</div>
                  <% } else if (req.query.error === 'invalid_data') { %>
                      <div class="alert alert-danger">Please provide a valid bid amount.</div>
                  <% } else if (req.query.error === 'server_error') { %>
                      <div class="alert alert-danger">An error occurred while processing your bid. Please try again.</div>
                  <% } %>
              <% } %>
      
              <div class="section-container">
      
                  <!-- LEFT: Available Projects -->
                  <div class="left-section">
                      <h2>Available Projects</h2>
                      <div class="bid-options">
                          <% if (bids && bids.length > 0) { %>
                              <% bids.forEach(function(bid) { %>
                                  <div class="bid-item" data-bid-id="<%= bid._id %>">
                                      <div class="bid-title"><%= bid.buildingType %> Project - <%= bid.totalFloors %> Floors</div>
                                      <div class="bid-price">Est. Budget: ₹<%= bid.estimatedBudget %></div>
                                      <div class="bid-description"><%= bid.specialRequirements || 'No special requirements provided.' %></div>
                                      <div class="bid-date">Posted: <%= new Date(bid.createdAt).toLocaleDateString('en-IN') %></div>
                                  </div>
                              <% }); %>
                          <% } else { %>
                              <div class="no-bids">No projects available for bidding at the moment.</div>
                          <% } %>
                      </div>
                  </div>
      
                  <!-- RIGHT: Detailed View + Form -->
                  <div class="right-section">
                      <% if (!selectedBid) { %>
                          <div class="bid-details-placeholder">
                              <p>Click on a project to view details and place your bid</p>
                          </div>
                      <% } else { %>
                          <div class="bid-details-content" style="display: block;">
                              <h2><%= selectedBid.buildingType %> Project - <%= selectedBid.totalFloors %> Floors</h2>
                              <div class="detail-price">Est. Budget: ₹<%= selectedBid.estimatedBudget %></div>
                              <div class="detail-description"><%= selectedBid.specialRequirements || 'No special requirements provided.' %></div>
                              <div class="detail-date">Posted: <%= new Date(selectedBid.createdAt).toLocaleDateString('en-IN') %></div>
      
                              <div class="additional-info">
                                  <h3>Additional Information</h3>
                                  <div class="info-item"><strong>Location:</strong> <%= selectedBid.projectLocation %></div>
                                  <div class="info-item"><strong>Total Area:</strong> <%= selectedBid.totalArea %> sq.ft</div>
                                  <div class="info-item"><strong>Timeline:</strong> <%= selectedBid.projectTimeline || 'Not specified' %> months</div>
                                  <div class="info-item"><strong>Accessibility Needs:</strong> <%= selectedBid.accessibilityNeeds || 'None' %></div>
                                  <div class="info-item"><strong>Energy Efficiency:</strong> <%= selectedBid.energyEfficiency || 'Standard' %></div>
                              </div>
      
                              <!-- Form -->
                              <div class="bid-form">
                                  <h3>Place Your Bid</h3>
                                  <form action="/submit-bid" method="POST">
                                      <div class="form-group">
                                          <label for="bid-amount">Your Bid Amount (₹):</label>
                                          <input type="number" id="bid-amount" name="bidPrice" min="0" step="10000" required>
                                      </div>
                                      <input type="hidden" name="bidId" value="<%= selectedBid._id %>">
                                      <input type="hidden" name="companyName" value="<%= companyName %>">
                                      <input type="hidden" name="companyId" value="<%= companyId %>">
                                      <button type="submit" class="btn-primary">Submit Bid</button>
                                  </form>
                              </div>
                          </div>
                      <% } %>
                  </div>
              </div>
          </div>
          <div id="bid-status" class="content-section">
            <h2>Your Bid Status</h2>
            <div class="bid-status-container">
    <% 
        // Filter out rejected bids and handle undefined case
        const visibleBids = (companyBids || []).filter(bid => bid.status !== 'rejected');
        if (visibleBids.length > 0) { 
    %>
        <% visibleBids.forEach(function(bid) { %>
            <div class="status-item">
                <div class="status-info">
                    <div class="status-title"><%= bid.project.buildingType %> Project - <%= bid.project.totalFloors %> Floors</div>
                    <div class="status-price">₹<%= bid.project.estimatedBudget %></div>
                    <div class="status-description"><%= bid.project.specialRequirements || 'No special requirements provided.' %></div>
                    <div class="status-date">Posted: <%= new Date(bid.project.createdAt).toLocaleDateString('en-IN') %></div>
                    <div class="your-bid">Your Bid: ₹<%= bid.bidPrice %></div>
                </div>
                <div class="status-badge <%= bid.status.toLowerCase() %>"><%= bid.status %></div>
            </div>
        <% }); %>
    <% } else { %>
        <div class="no-bids">You haven't placed any bids yet.</div>
    <% } %>
</div>
        </div>
        </div>
    </div>

    <script>
document.addEventListener('DOMContentLoaded', function() {
    // Navigation handling
    const navItems = document.querySelectorAll('.nav-item');
    const contentSections = document.querySelectorAll('.content-section');

    navItems.forEach(item => {
        item.addEventListener('click', function() {
            const targetSection = this.getAttribute('data-section');
            
            navItems.forEach(navItem => navItem.classList.remove('active'));
            this.classList.add('active');
            
            contentSections.forEach(section => {
                section.classList.remove('active');
                if (section.id === targetSection) {
                    section.classList.add('active');
                }
            });
        });
    });

    // Bid selection handling - now using server-rendered data
    const bidItems = document.querySelectorAll('.bid-item');
    
    bidItems.forEach(item => {
        item.addEventListener('click', function(e) {
            // Remove active class from all bid items
            bidItems.forEach(bidItem => bidItem.classList.remove('active'));
            // Add active class to clicked item
            this.classList.add('active');
            
            // The actual navigation is handled by the link in the HTML
            // No need for additional JavaScript as the page will reload
        });
    });

    // Form submission handling
    const bidForm = document.querySelector('.bid-form form');
    if (bidForm) {
        bidForm.addEventListener('submit', function(e) {
            e.preventDefault();
            
            const bidAmountInput = document.getElementById('bid-amount');
            const bidAmount = parseFloat(bidAmountInput.value);
            
            if (!bidAmount || isNaN(bidAmount)) {
                alert('Please enter a valid bid amount');
                return;
            }
            
            // Additional validation can be added here if needed
            
            // Submit the form
            this.submit();
        });
    }

    // Status badge tooltips
    const statusBadges = document.querySelectorAll('.status-badge');
    statusBadges.forEach(badge => {
        badge.addEventListener('mouseenter', function() {
            const status = this.classList.contains('pending') ? 'pending' : 
                          this.classList.contains('accepted') ? 'accepted' : 'rejected';
            
            let statusMessage = '';
            switch(status) {
                case 'pending':
                    statusMessage = 'Your bid is under review';
                    break;
                case 'accepted':
                    statusMessage = 'Congratulations! Your bid was accepted';
                    break;
                case 'rejected':
                    statusMessage = 'Sorry, your bid was not accepted';
                    break;
            }
            
            this.setAttribute('title', statusMessage);
        });
    });
});
// Bid selection handling
const bidItems = document.querySelectorAll('.bid-item');

bidItems.forEach(item => {
    item.addEventListener('click', function(e) {
        // Remove active class from all bid items
        bidItems.forEach(bidItem => bidItem.classList.remove('active'));
        // Add active class to clicked item
        this.classList.add('active');
        
        // Get the bid ID from the data attribute
        const bidId = this.getAttribute('data-bid-id');
        
        // Navigate to the same page with the bid ID as a query parameter
        window.location.href = `/companybids?bidId=${bidId}`;
    });
});
    </script>
</body>
</html>