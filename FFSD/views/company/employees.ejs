<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Employees</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-1ycn6IcaQQ40/MKBW2W4Rhis/DbILU74C1vSrLJxCq57o941Ym01SwNsOMqvEBFlcgUa6xLiPY/NS5R+E6ztJQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        :root {
            --primary-accent: #004D7A;       /* Deep Navy Blue for Authority/Trust */
            --primary-accent-hover: #003355; /* Darker Navy for interaction */
            --background-light: #F8F9FA;     /* Very light, soft grey background */
            --content-light: #FFFFFF;        /* Pure white for cards */
            --text-primary: #212529;         /* Dark charcoal for main text */
            --text-secondary: #6C757D;       /* Medium grey for labels */
            --highlight-color: #A06700;      /* Muted Gold/Amber for emphasis/pending */
            --border-light: #D1D5DB;         /* Clean, soft border */
            --card-radius: 8px;
            --soft-shadow: 0 4px 12px rgba(0, 0, 0, 0.08);
            --button-secondary-bg: #E9ECEF;  /* Light grey for secondary buttons */
            --status-success-bg: #D4EDDA;    
            --status-success-text: #155724;  
            --status-warning-bg: #FFF3CD;    
            --status-warning-text: #856404;  
            --status-danger-bg: #f8d7da;
            --status-danger-text: #721c24;
            --danger-color: #dc3545;
            --danger-hover: #bd2130;
            --star-color: #f4a261;
            --background-dark: var(--background-light);
            --content-dark: var(--content-light);
            --border-dark: var(--border-light);
            --text-dark: var(--text-primary);
            --shadow: var(--soft-shadow);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: var(--background-dark);
            color: var(--text-primary);
            line-height: 1.6;
            font-size: 15px;
            -webkit-font-smoothing: antialiased;
        }

        .container {
            width: 90%;
            max-width: 1200px;
            margin: 3rem auto; /* Increased margin for better spacing */
            padding: 0 2rem;
        }

        .page-header h1 {
            font-size: 2rem; /* Adjusted for consistency with dashboard */
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 2rem;
            border-bottom: 2px solid var(--primary-accent);
            padding-bottom: 1rem;
        }

        .employee-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(360px, 1fr)); /* Aligned with dashboard card width */
            gap: 2rem; /* Increased gap for professional spacing */
        }

        .employee-card {
            background-color: var(--content-light);
            border: 1px solid var(--border-light);
            border-radius: var(--card-radius);
            box-shadow: var(--soft-shadow);
            display: flex;
            flex-direction: column;
            transition: var(--transition);
            overflow: hidden;
            min-height: 380px; /* Fixed height for consistency */
            max-width: 400px; /* Set max-width for professional appearance */
        }

        .employee-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
            border-color: var(--primary-accent);
        }

        .card-header {
            display: flex;
            align-items: center;
            padding: 1.75rem; /* Increased padding for spaciousness */
            border-bottom: 1px solid var(--border-light);
            background-color: var(--primary-accent);
            color: var(--content-light);
        }

        .profile-image {
            width: 90px; /* Slightly larger for prominence */
            height: 90px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--content-light);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-right: 1.5rem;
            transition: var(--transition);
        }

        .profile-image:hover {
            transform: scale(1.05);
        }

        .header-info h3 {
            font-size: 1.3rem; /* Slightly larger for emphasis */
            margin: 0;
            font-weight: 600;
        }

        .header-info .title {
            color: rgba(255, 255, 255, 0.8); /* Adjusted for header background */
            font-size: 0.95rem;
            font-weight: 400;
        }

        .card-body {
            padding: 1.75rem; /* Increased padding for better spacing */
            flex-grow: 1;
        }

        .detail-item {
            display: flex;
            align-items: center;
            color: var(--text-secondary);
            margin-bottom: 0.75rem;
            font-size: 0.95rem; /* Slightly larger for readability */
        }

        .detail-item i {
            width: 20px;
            text-align: center;
            margin-right: 10px;
            color: var(--primary-accent);
        }

        .detail-item span {
            color: var(--text-primary);
            font-weight: 500;
        }

        .availability {
            text-transform: capitalize;
            font-weight: 600;
        }

        .availability.available { 
            color: var(--status-success-text); 
            background: var(--status-success-bg); 
            padding: 0.25rem 0.5rem; 
            border-radius: 12px; 
        }
        .availability.busy { 
            color: var(--status-warning-text); 
            background: var(--status-warning-bg); 
            padding: 0.25rem 0.5rem; 
            border-radius: 12px; 
        }
        .availability.unavailable { 
            color: var(--status-danger-text); 
            background: var(--status-danger-bg); 
            padding: 0.25rem 0.5rem; 
            border-radius: 12px; 
        }

        .card-footer {
            padding: 1.25rem 1.75rem; /* Aligned with dashboard */
            background-color: var(--button-secondary-bg);
            text-align: center;
            border-top: 1px solid var(--border-light);
        }

        .btn-chat {
            background-color: var(--primary-accent);
            color: var(--content-light);
            padding: 0.75rem 1.5rem;
            border-radius: 6px; /* Aligned with dashboard buttons */
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            box-shadow: 0 4px 10px rgba(0, 77, 122, 0.2);
        }

        .btn-chat:hover {
            background-color: var(--primary-accent-hover);
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(0, 77, 122, 0.3);
        }

        .no-employees {
            text-align: center;
            padding: 3rem;
            color: var(--text-secondary);
            font-size: 1.1rem;
            background: var(--content-light);
            border-radius: var(--card-radius);
            box-shadow: var(--soft-shadow);
            border: 1px solid var(--border-light);
        }

        @media (max-width: 1024px) {
            .container { 
                padding: 0 1.5rem; 
            }
            .employee-grid {
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            }
        }

        @media (max-width: 992px) {
            .employee-grid {
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            }
        }

        @media (max-width: 768px) {
            .container { 
                padding: 0 1rem; 
            }
            .page-header h1 {
                font-size: 1.8rem;
            }
            .card-header {
                flex-direction: column;
                align-items: flex-start;
                text-align: left;
            }
            .profile-image {
                margin-bottom: 1rem;
                margin-right: 0;
            }
        }

        @media (max-width: 576px) {
            .employee-grid {
                grid-template-columns: 1fr;
            }
            .profile-image {
                width: 70px;
                height: 70px;
            }
        }
    </style>
</head>
<body>
    <%- include('navbar_company') %>
    <div class="container">
        <div class="page-header">
            <h1>My Employees</h1>
        </div>
        <div class="employee-grid">
            <% if (employees && employees.length > 0) { %>
                <% employees.forEach(employee => { %>
                    <div class="employee-card">
                        <div class="card-header">
                            <img src="<%= employee.worker.profileImage || 'https://via.placeholder.com/100' %>" alt="Profile" class="profile-image">
                            <div class="header-info">
                                <h3><%= employee.worker.name %></h3>
                                <p class="title"><%= employee.worker.professionalTitle || 'Worker' %></p>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="detail-item">
                                <i class="fas fa-envelope"></i>
                                <span><%= employee.worker.email %></span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-phone"></i>
                                <span><%= employee.worker.phone %></span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-tools"></i>
                                <span><%= employee.worker.specialization %></span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-briefcase"></i>
                                <span><%= employee.worker.experience %> years of experience</span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-calendar-check"></i>
                                <span class="availability <%= employee.worker.availability %>"><%= employee.worker.availability %></span>
                            </div>
                        </div>
                        <div class="card-footer">
                            <a href="/chat/<%= employee.chatId %>" class="btn-chat">
                                <i class="fas fa-comment-dots"></i> Chat Now
                            </a>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p class="no-employees">You have no employees yet.</p>
            <% } %>
        </div>
    </div>
</body>
</html>